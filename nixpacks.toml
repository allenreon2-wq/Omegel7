[phases.setup]
nixPkgs = ["python311"]

[start]
cmd = "python bot.py"
```
Commit ✅

---

## ✅ Fix 3 — Railway mein manually redeploy karo

Railway → apni service → **"Deployments"** tab → upar **"···"** (3 dots) → **"Redeploy"**

---

## ✅ Fix 4 — Agar phir bhi na ho, Railway mein variable add karo

Railway → **Variables** tab → yeh add karo:
```
NIXPACKS_PYTHON_VERSION = 3.11.8
