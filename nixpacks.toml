[phases.setup]
nixPkgs = ["python311", "gcc"]

[start]
cmd = "python bot.py"
```

**Commit karo** ✅

---

Saath mein `requirements.txt` bhi update karo — file open karo → **pencil icon** → content change karo:
```
pyrogram==2.0.106
TgCrypto==1.2.5
aiosqlite==0.19.0
python-dotenv==1.0.1
```

Commit karo ✅

---

Railway automatically redeploy karega. Is baar logs mein yeh dikhega:
```
✅ Database connected
🚀 Starting bot...
✅ Bot started as @Omegel_Unknown_Chats7_Bot
